<h2 id="title">Welcome to Ember.js</h2>
{{#if session.isAuthenticated}}
  <a {{ action 'invalidateSession' }}>{{session.currentUser.fullName}} Logout</a>
{{else}}
  {{#link-to 'login'}}Login{{/link-to}}
{{/if}}
<br/>
{{outlet}}